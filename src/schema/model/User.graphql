type User implements Node {
  id: ID!
  firstName: String
  lastName: String
  email: String
  provider: String
  photoUrl: String
  providerId: String
}

input CreateUserInput {
  firstName: String!
  lastName: String!
  email: String!
  password: String!
  photoUrl: String
}