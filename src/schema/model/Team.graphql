enum Program {
  JFLL
  FLL
  FTC
  FRC
}

type Team implements Node {
  id: ID!
  program: Program!
  number: Int!
  homeCmp: String
  nameFull: String
  nameShort: String
  schoolName: String
  city: String
  stateProv: String
  country: String
  rookieYear: Int
  robotName: String
  districtCode: String
  website: String
}

type TeamEdge {
  cursor: String!
  node: Team
}

type TeamConnection {
  edges: [TeamEdge]
  pageInfo: PageInfo!
}